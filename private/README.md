# Private data mount point

This directory is ignored by Git except for this file. Keep candidate profiles,
CV sources, job descriptions, trackers, generated documents, databases, task
inputs, and credentials here (or point `workspace.json` at a location outside
the repository).

Do not add real data to `examples/` as a substitute.
