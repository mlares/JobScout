# Career App

The reviewed application source will be migrated here in a later step. It must
use `workspace.json` or `CAREER_PRIVATE_ROOT`; it must not embed a user-specific
filesystem path or profile.
